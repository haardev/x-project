import React from 'react';
import { ChatContainer } from "../components/ChatContainer";

const Home = () => {
    return (
        <div className="home-page">
            <ChatContainer/>
        </div>
    );
};

export default Home;
