import * as React from 'react';

type Props = {};

export const UsersList = (props: Props) => {
    return (
        <div className="chat-user-list">
            <ul>
                <li>Milen user</li>
            </ul>
        </div>
    );
};
